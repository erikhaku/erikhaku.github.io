
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content="Astro" />
    <title>Haku Holding AB</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Helvetica', 'Arial', sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-color: white;
        color: #333;
        text-align: center;
        padding: 2rem;
      }

      .image-container {
        margin-bottom: 1rem;
        position: relative;
        overflow: hidden; /* Important for rounded corners to work correctly */
        width: 450px;
        height: auto;
        border-radius: 32px;
      }

      .image-container img {
        max-width: 100%;
        height: auto;
        border-radius: 32px; /* Adjust the radius for desired roundness */
        display: block;
        transition: opacity 0.3s ease-in-out; /* Adding transition for smooth fade effect */
      }
      
      .hover-image {
        position: absolute;
        top: 0;
        left: 0;
        opacity: 0;
        width: 100%;
        height: auto;
      }
      
      .image-container:hover .default-image {
        opacity: 0;
      }
      
      .image-container:hover .hover-image {
        opacity: 1;
      }

      .text-content {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

	  .header-content {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.header-content img {
  width: 40px;
  height: 40px;
  margin-bottom: 20px;
}


	  .header-content h1 {
  margin-right: 16px;
	  }

      h1 {
        font-size: 2rem;
        font-weight: bold;
        opacity: 0.9;
        margin-bottom: 1.2rem;
      }

      .subtitle {
        font-size: 1.2rem;
        margin-bottom: 0.2rem;
        color: #666;
      }

      .contact {
        font-size: 1rem;
        color: #999;
      }
    </style>
  </head>
  <body>
    <div class="header-content">
      <h1>Haku Holding AB</h1>
	  <img class="default-image" width="40"  src="favicon.svg" alt="hakulogo" />
    </div>
    <div class="image-container">
      <img class="default-image" width="450" src="profile.png" alt="haku holding AB" />
      <img class="hover-image" width="450" src="profile_hover.png" alt="haku holding AB" />
    </div>
    
    <div class="text-content">
      <div class="subtitle">Erik Johansson</div>
      <div class="contact">erik@hakuholding.se</div>
    </div>
  </body>
</html>